#include "traceline/telemetry_bus.hpp"

// TelemetryBus is header-only (template class) -- this translation
// unit exists so the target has a stable .cpp for tooling (clang-tidy,
// coverage) that expects at least one source file per header.
