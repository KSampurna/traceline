# Reference only -- documents the variable name .mcp.json expects.
# Claude Code does NOT auto-load this file; export the real value in
# your shell (or via direnv/.envrc, also gitignored) before launching
# Claude Code. Never commit the actual token.

GITHUB_PERSONAL_ACCESS_TOKEN=your-github-pat-here
